import React from 'react';
import ReactDOM from 'react-dom/client';
import RamblApp from './RamblApp';
import './RamblApp.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<RamblApp />);